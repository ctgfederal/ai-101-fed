# Steering References

The SDD links to two steering docs in Context References:

| In SDD | Steering anchor |
|---|---|
| Tech stack | `.claude/steering/tech.md` |
| Project structure | `.claude/steering/structure.md` |

If either is missing, halt SDD generation and tell the user to run `steering-docs-creator`.
