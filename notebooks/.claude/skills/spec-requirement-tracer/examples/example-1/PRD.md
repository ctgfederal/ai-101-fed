# PRD: Feature Search

## Functional Requirements

- **REQ-001**: WHEN a user submits a query THEN the system SHALL return ranked results within 200ms.
- **REQ-002**: The system SHALL paginate results 20 per page.
- **REQ-003**: The system SHALL log every query for analytics.
