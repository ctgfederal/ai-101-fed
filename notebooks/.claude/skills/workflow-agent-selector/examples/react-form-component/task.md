# Task

Build a `RegisterForm` React component using React Hook Form and Zod for
validation. The component must call our existing Supabase auth client and
display loading + error states. Style with Tailwind. Match the existing
pattern in `components/forms/LoginForm.tsx`.
