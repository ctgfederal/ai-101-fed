# Knowledge — spec-execution

Index of reference files. Load only the file relevant to the current step.

| File | When to load |
|------|--------------|
| `tdd-loop.md` | When entering the red→green→refactor cycle for any task. Defines what counts as "done". |
| `state-schema.md` | When inspecting, debugging, or extending `execution-state.json`. Defines the JSON shape. |
| `interruption-recovery.md` | When resuming a paused run, recovering from a crash, or unblocking a task that was marked `blocked`. |
