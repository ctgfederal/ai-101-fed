---
name: Josh prefers explicit error types over Result wrappers — keeps stack traces clean and the type system honest.
description: Josh prefers explicit error types over Result wrappers — keeps stack traces clean and the type system honest.
type: user
---

Josh prefers explicit error types over Result wrappers — keeps stack traces clean and the type system honest.
