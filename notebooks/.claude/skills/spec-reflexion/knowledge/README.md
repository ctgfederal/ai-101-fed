# spec-reflexion — knowledge

| File | Contents | When to load |
|---|---|---|
| `learning-types.md` | Definitions of the four memory types (`feedback`, `project`, `reference`, `user`) and which one to pick | When choosing a `--type` for `promote_to_memory.py` |
| `local-vs-global.md` | Heuristics that drive `classify_learning.py`; what stays in spec README vs. promotes to memory | When a learning is borderline and the user wants to know why it was classified one way |
| `memory-format.md` | Exact frontmatter schema and MEMORY.md index conventions; matches Claude Code's auto-memory protocol | When inspecting or extending the memory output format |
