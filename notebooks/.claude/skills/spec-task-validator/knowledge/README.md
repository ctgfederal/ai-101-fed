# spec-task-validator — knowledge

| File | Contents | When to load |
|---|---|---|
| `task-schema.md` | Required task fields, ID format, allowed phases / TDD step values, reference syntax | When extending the parser, debugging a parse failure, or explaining "what is a task" |
| `acceptance-rubric.md` | What counts as "measurable" acceptance | When narrating why a task got `fail` for non-measurable acceptance, or tuning the observation tokens |
| `verdict-thresholds.md` | Per-task status rules + overall PLAN verdict | When narrating the verdict, or interpreting a borderline case |
